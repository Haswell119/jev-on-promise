# data/

Datasets used to train, calibrate and sanity-check Sextant (a non-neural decision engine).

| directory | contents | committed? |
|---|---|---|
| `synthetic/` | synthetic records generated by `scripts/synth/generate.py` (labels verifiable by construction) | yes |
| `raw/` | downloaded source files (cache) | **no** (gitignored) |
| `processed/<dataset>/{train,calib,dev}.jsonl` | records built from public datasets by `scripts/prepare_data.py` | **no** (gitignored) |
| `provenance.jsonl` | one line per dataset: source, revision, license, files, splits, row counts, notes | yes |

**Raw and processed real-data files are not committed.** They are re-created deterministically (seed 20260922) by running `python3 scripts/prepare_data.py` (requires `pip install requests huggingface_hub pandas pyarrow` and outbound HTTPS). Each dataset's license is re-verified from its dataset card / LICENSE file at download time; the script refuses datasets whose license cannot be verified as permissive and hard-blocks every benchmark corpus listed in `FORBIDDEN_SOURCES`.

## Regenerating

```bash
python3 scripts/prepare_data.py                         # all datasets
python3 scripts/prepare_data.py --datasets snips,snli  # a subset
python3 scripts/prepare_data.py --limit 20             # quick smoke test (caps every split at 20 rows)
python3 scripts/prepare_data.py --verify-only          # re-validate data/processed without downloading
```

Large parquet shards are not downloaded in full: the script reads an evenly spaced subset of their row groups over HTTP range requests and caches that subset under `data/raw/`. Hub revisions are pinned in `PINNED_REVISIONS` (`--latest` overrides). Sampling is label-balanced where the source allows it, with caps of 1200 / 400 / 400 source rows per split (train / calib / dev); datasets with several transformations emit one record per transformation for each sampled row, linked by the `group` field.

## Record format

One JSON object per line: `{id, source, license, split, tier: "external_dev", family, synthetic: false, transformation, group, request: {model: "sextant-1", state, questions: {q: {type: choice|score|noul, instructions, criteria}}}, gold: {q: ...}}`. Choice `criteria` maps option key to an authored description, score `criteria` is the ordered list of levels (gold = level index), noul gold is a boolean. States are kept under ~4000 characters (long fields are cut at a sentence boundary and end with ` [truncated]`). Option descriptions were written for this project and are not part of the source datasets.

## Datasets

| dataset | source | license | family | transformations | train / calib / dev (records) | notes |
|---|---|---|---|---|---|---|
| `hwu64` | https://github.com/xliuhw/NLU-Evaluation-Data | CC BY 4.0 (Attribution 4.0 International) | intent | 10_options, full_options | 2400 / 800 / 800 (1189 / 399 / 395 rows) | Filtered: status starting with IRR (irrelevant) dropped, only the standard 64 intents kept (audio_volume_other, cooking_query, general_greet, music_dislikeness excluded), empty answers dropped, exact-duplicate utterances removed. Dropped counts: {'status_IRR*': 642, 'not_in_64:general_greet': 25, 'not_in_64:music_dislikeness': 25, 'not_in_64:audio_volume_other': 24, 'not_in_64:cooking_query': 6, 'empty_answer': 30}. Two records per utterance: full_options (64) and 10_options (gold + 9 seeded random intents), linked via `group`. |
| `snips` | https://github.com/sonos/nlu-benchmark | CC0 1.0 Universal | intent | full_options | 1200 / 400 / 400 | 2017-06-custom-intent-engines, 7 intents; entity spans flattened to plain text; duplicates removed. |
| `dbpedia_14` | fancyzhx/dbpedia_14 | cc-by-sa-3.0 (Creative Commons Attribution-ShareAlike 3.0) | topic | full_options | 1200 / 400 / 400 | The source parquet files are class-sorted; evenly spaced row groups were read to cover all 14 classes. |
| `snli` | stanfordnlp/snli | cc-by-sa-4.0 (Creative Commons Attribution-ShareAlike 4.0 International) | compatibility | choice, noul | 2400 / 800 / 800 (1200 / 400 / 400 rows) | Rows with label -1 (no gold) dropped: {'train:label_-1': 785, 'validation:label_-1': 158}. Two records per pair (choice + noul; noul gold = entailment, so about one third of noul golds are true), linked via `group`. |
| `amazon_polarity` | fancyzhx/amazon_polarity | apache-2.0 (Apache License 2.0) | sentiment | choice, noul | 2400 / 800 / 800 (1200 / 400 / 400 rows) | Two records per review (noul + choice), linked via `group`. |
| `tweet_eval_sentiment` | cardiffnlp/tweet_eval (config: sentiment) | Creative Commons Attribution 3.0 Unported (sentiment config only; Twitter ToS apply) | sentiment | full_options | 1200 / 400 / 400 | Only the `sentiment` config is used (other configs are Undefined / need permission). |
| `prosocial_dialog` | allenai/prosocial-dialog | cc-by-4.0 | severity | score | 1200 / 400 / 400 | Score levels = the dataset's ordered safety_label scale (0 casual .. 4 needs intervention). The three per-annotator votes use a 3-way scale (casual / needs caution / needs intervention) from which the 5-level label is derived deterministically, so no annotator distribution over the 5 levels exists; the raw vote counts are kept in the extra field `annotator_votes` instead of `gold_probs`. Contexts were GPT-3-generated, labels are human (per the card). Dropped: {}. |
| `pubmedqa` | qiaojin/PubMedQA (config: pqa_labeled) | mit (MIT License) | factual_yes_no | noul | 534 / 178 / 178 | Only yes/no rows kept (maybe dropped): {'final_decision:maybe': 110}. Gold = final_decision == yes; context = abstract sections joined with a space. dev/calib are yes/no balanced; train keeps the remaining rows (yes-heavy). |
| `squad_v2` | rajpurkar/squad_v2 | cc-by-sa-4.0 (CC BY-SA 4.0) | adequacy | noul | 1200 / 400 / 400 | Gold = question has at least one answer span. Balanced answerable/unanswerable. Dropped: {}. |
| `bitext_support` | bitext/Bitext-customer-support-llm-chatbot-training-dataset | cdla-sharing-1.0 (Community Data License Agreement - Sharing 1.0) | intent+routing | category_options, intent_options | 2400 / 800 / 800 (1200 / 400 / 400 rows) | Hybrid synthetic (NLG-expanded from natural seeds, per the card); {{placeholders}} kept verbatim; exact duplicates removed. Two records per utterance (27-intent choice + 11-category choice), linked via `group`. |
| `cfpb_complaints` | BEE-spoke-data/consumer-finance-complaints (config: has-text) | cc0-1.0 (CC0 1.0 Universal; US government data) | routing | product_options | 1200 / 400 / 400 | Only rows with a narrative. Product values were mapped onto the current CFPB taxonomy (mapped: {'Credit reporting, credit repair services, or other personal consumer reports -> Credit reporting or other personal consumer reports': 19257, 'Credit card or prepaid card -> Credit card': 2106, 'Payday loan, title loan, or personal loan -> Payday loan, title loan, personal loan, or advance loan': 361, 'Credit card or prepaid card -> Prepaid card': 208, 'Money transfers -> Money transfer, virtual currency, or money service': 3, 'Credit reporting -> Credit reporting or other personal consumer reports': 64, 'Bank account or service -> Checking or savings account': 39, 'Payday loan -> Payday loan, title loan, personal loan, or advance loan': 4}); options = the 10 canonical products with >= 30 rows in the subset (rare products dropped: {'Debt or credit management': 19}); dropped rows: {'unmappable:Consumer Loan': 25, 'unmappable:Other financial service': 1}. Narratives keep the CFPB XXXX redactions. |
| `medical_abstracts` | TimSchopf/medical_abstracts | cc-by-sa-3.0 (Creative Commons Attribution-ShareAlike 3.0 Unported) | topic | full_options | 1200 / 400 / 400 | Exact-duplicate abstracts removed (the source has ~18% duplicates) and dev texts excluded from the train pool. |

Generated by `scripts/prepare_data.py` on 2026-09-22. See `provenance.jsonl` for revisions, file hashes and label counts.
