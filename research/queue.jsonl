{"id": "E1", "hypothesis": "H1: compact pretrained cross-encoder over (question, candidate, retrieved evidence) beats the symbolic champion", "status": "running", "train_args": "--train data/neural/bench_train.jsonl data/neural/train_v1.jsonl --dev data/neural/bench_dev.jsonl --encoder sentence-transformers/all-MiniLM-L6-v2 --pooling mean --epochs 0.45 --max-len 192 --group-batch 4 --max-candidates 6 --lr 5e-5 --head-lr 1e-3 --dev-limit 1000 --eval-every 1000 --save-every 500 --max-seconds 9000 --seed 20260923"}
{"id": "E2", "hypothesis": "H2: hybrid head consuming the symbolic feature vector and fusion logit beats the pure neural scorer", "status": "pending", "train_args": "--train data/neural/bench_train_feat.jsonl --dev data/neural/bench_dev_feat.jsonl --encoder sentence-transformers/all-MiniLM-L6-v2 --pooling mean --use-features --use-symbolic-logit --epochs 0.45 --max-len 192 --group-batch 4 --max-candidates 6 --lr 5e-5 --head-lr 1e-3 --dev-limit 1000 --eval-every 1000 --save-every 500 --max-seconds 9000 --seed 20260923", "notes": "Uses the R2 retriever (state-local idf + question expansion); training pairs must be re-exported with --local-idf --q-expand and scorer.json must record both flags.", "export_flags": "--local-idf --q-expand", "after": "E1b or E1, whichever retriever wins"}
{"id": "E3", "hypothesis": "H10: a 12-layer encoder (33M) buys accuracy worth the CPU latency", "status": "pending", "train_args": "--train data/neural/bench_train.jsonl data/neural/train_v1.jsonl --dev data/neural/bench_dev.jsonl --encoder sentence-transformers/all-MiniLM-L12-v2 --pooling mean --epochs 0.25 --max-len 192 --group-batch 4 --max-candidates 6 --lr 4e-5 --head-lr 1e-3 --dev-limit 1000 --eval-every 800 --save-every 400 --max-seconds 9000 --seed 20260923", "notes": "Uses the R2 retriever (state-local idf + question expansion); training pairs must be re-exported with --local-idf --q-expand and scorer.json must record both flags.", "export_flags": "--local-idf --q-expand", "after": "E1b or E1, whichever retriever wins"}
{"id": "E4", "hypothesis": "H3+H6: wider evidence budget (260 words, max-len 288) plus Brier-regularised objective improves long context and calibration", "status": "pending", "train_args": "--train data/neural/bench_train_w260.jsonl --dev data/neural/bench_dev_w260.jsonl --encoder sentence-transformers/all-MiniLM-L6-v2 --pooling mean --epochs 0.35 --max-len 288 --group-batch 3 --max-candidates 6 --lr 5e-5 --head-lr 1e-3 --brier-weight 0.3 --dev-limit 1000 --eval-every 800 --save-every 400 --max-seconds 9000 --seed 20260923", "notes": "Uses the R2 retriever (state-local idf + question expansion); training pairs must be re-exported with --local-idf --q-expand and scorer.json must record both flags.", "export_flags": "--local-idf --q-expand", "after": "E1b or E1, whichever retriever wins"}
{"id": "P1", "status": "pending", "kind": "probe", "after": "E1", "hypothesis": "H16/H17 raise evidence recall; does that translate into decision accuracy? Score the trained E1 model over dev pairs re-exported with the R2 retriever.", "design": "Cheap probe, minutes not hours: sextant neural-probe with models/E1/neural over data/neural/bench_dev_r2.jsonl (exported with --local-idf --q-expand) versus the parity export. Train/inference parity is deliberately broken, so the result is directional only: an accuracy GAIN despite the mismatch is strong evidence for the retriever and justifies the full E1b run; a loss is ambiguous and E1b is skipped.", "command": "./target/release/sextant neural-probe --neural-dir models/E1/neural --input data/neural/bench_dev_r2.jsonl --out reports/challengers/E1.r2probe.jsonl", "cost": "~5 min CPU"}
{"id": "E1b", "status": "conditional", "after": "P1", "hypothesis": "H16/H17: training on evidence retrieved with the state-local idf and the expanded question query improves decision accuracy, not just recall.", "design": "Identical to E1 in every hyperparameter, seed and schedule; the ONLY difference is that every pair is exported with --local-idf --q-expand and scorer.json records both flags. Run only if P1 is positive.", "export_flags": "--local-idf --q-expand", "cost": "~3 h CPU"}
